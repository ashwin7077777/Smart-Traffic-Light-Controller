class driver;

virtual traffic_if vif;

mailbox gen2drv;

function new(
virtual traffic_if vif,
mailbox gen2drv
);

this.vif = vif;
this.gen2drv = gen2drv;

endfunction

task run();

traffic_transaction tr;

forever
begin

gen2drv.get(tr);

@(posedge vif.clk);

end

endtask

endclass
